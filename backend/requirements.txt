# Runtime dependencies for the dreamforge backend.
#
# All dependencies for every phase live here so a single
# `pip install -r requirements.txt` is enough to run the project from any
# phase forward. Phase 1 only needs config + parsing + chunking + Chroma +
# embeddings; later phases use the rest.

# --- Web layer (Phase 1+) ---
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.20

# --- Configuration (Phase 1+) ---
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0

# --- Document parsing (Phase 1+) ---
pdfplumber>=0.11

# --- Vector store (Phase 1+) ---
chromadb>=0.5

# --- Embeddings (Phase 1+) ---
sentence-transformers>=3.0

# --- LLM provider (Phase 2+, used for theme labels / dream / explain) ---
groq>=0.13

# --- Semantic graph engine (Phase 2+) ---
scikit-learn>=1.5
hdbscan>=0.8.40
umap-learn>=0.5.6
networkx>=3.3
numpy>=1.26
